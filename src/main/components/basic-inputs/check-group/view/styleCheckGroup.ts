// internal imports
import defineStyle from '../../../../styling/defineStyle'

// --- styleCheckGroup ----------------------------------------------

const styleCheckGroup = defineStyle(theme => ({
  container: {
    margin: '0.5rem 0 0.5rem 0',
  },

  label: {
  },

  option: {
    margin: '10px 0 0 0'
  }
}))

// --- exports ------------------------------------------------------

export default styleCheckGroup
