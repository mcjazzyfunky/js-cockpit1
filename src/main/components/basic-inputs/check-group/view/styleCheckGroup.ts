// internal imports
import defineStyle2 from '../../../../styling/defineStyle2'

// --- styleCheckGroup ----------------------------------------------

const styleCheckGroup = defineStyle2(theme => ({
  container: {
  },

  label: {
  },

  option: {
    margin: '10px 0 0 0'
  }
}))

// --- exports ------------------------------------------------------

export default styleCheckGroup
