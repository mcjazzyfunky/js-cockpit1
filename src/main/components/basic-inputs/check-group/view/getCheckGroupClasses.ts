// internal imports
import defineStyles from '../../../../styling/defineStyles'

// --- getCheckGroupClasses ------------------------------------------

const getCheckGroupClasses = defineStyles(() => ({
  container: {
  },

  label: {
  },

  option: {
    margin: '10px 0 0 0'
  }
}))

// --- exports -------------------------------------------------------

export default getCheckGroupClasses
