// internal imports
import defineStyles from '../../../../styling/defineStyles'

// --- getTextInputClasses -------------------------------------------

const getTextInputClasses = defineStyles(theme => ({
  container: {
  },

  label: {
    // fontSize: theme.fonts.smallPlus.fontSize,
    // fontFamily: theme.fonts.smallPlus.fontFamily,
    // margin: '0 0 1px 0',
  },
}))

// --- exports -------------------------------------------------------

export default getTextInputClasses
