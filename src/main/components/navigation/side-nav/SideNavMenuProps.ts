// external imports
import { ReactNode } from 'react'

// --- SideNavMenuProps ---------------------------------------------

type SideNavMenuProps = {
  text: string,
  menuId?: string,
  children: ReactNode // TODO
}

// --- exports ------------------------------------------------------

export default SideNavMenuProps
