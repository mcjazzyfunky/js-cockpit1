// external imports
import { ReactNode } from 'react'

// --- SideNavItemsProps --------------------------------------------

type SideNavItemProps = {
  title: string,
  id: string,
}

// --- exports ------------------------------------------------------

export default SideNavItemProps
