// external imports
import { ReactNode } from 'react'

// --- SideNavItemProps ---------------------------------------------

type SideNavItemProps = {
  text: string,
  id: string,
}

// --- exports ------------------------------------------------------

export default SideNavItemProps
