// external imports
import { CSSProperties, ReactNode } from 'react'

// --- UserMenuProps ------------------------------------------------

type UserMenuProps = {
  username?: string,
  fullName?: string
}

// --- exports ------------------------------------------------------

export default UserMenuProps
