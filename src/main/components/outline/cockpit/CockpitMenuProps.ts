// external imports
import { CSSProperties, ReactNode } from 'react'

// --- CockpitMenuProps ---------------------------------------

type CockpitMenuProps = {
  className?: string,
  style?: CSSProperties,
  children?: ReactNode // TODO
}

// --- exports ------------------------------------------------------

export default CockpitMenuProps
