// --- PaginationInfoProps -------------------------------------------

type PaginationInfoProps = {
  pageIndex: number,
  pageSize: number,
  totalItemCount: number,
  about: 'items'
}

// --- export --------------------------------------------------------

export default PaginationInfoProps
