// externals imports
import { CSSProperties, ReactNode } from 'react'

// --- VBoxProps -----------------------------------------------------

type VBoxProps = {
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports -------------------------------------------------------

export default VBoxProps
