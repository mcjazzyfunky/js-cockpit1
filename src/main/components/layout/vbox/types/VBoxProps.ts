// externals imports
import { CSSProperties, ReactNode } from 'react'

// --- VBoxCellProps ------------------------------------------------

type VBoxProps = {
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports ------------------------------------------------------

export default VBoxProps
