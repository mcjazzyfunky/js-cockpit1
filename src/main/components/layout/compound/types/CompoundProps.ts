// external imports
import React, { CSSProperties, ReactNode } from 'react'

// --- CompoundProps -------------------------------------------------

type CompoundProps = {
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports -------------------------------------------------------

export default CompoundProps
