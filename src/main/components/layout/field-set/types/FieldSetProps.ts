// external imports
import React, { CSSProperties, ReactNode } from 'react'

// --- FieldSetProps -------------------------------------------------

type FieldSetProps = {
  title?: string,
  grow?: number,
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports ------------------------------------------------------

export default FieldSetProps
