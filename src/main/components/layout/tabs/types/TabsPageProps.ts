// external imports
import React, { CSSProperties, ReactNode } from 'react'

// --- TabsPageProps ------------------------------------------------

type TabsPageProps = {
  title?: string,
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports ------------------------------------------------------

export default TabsPageProps 
