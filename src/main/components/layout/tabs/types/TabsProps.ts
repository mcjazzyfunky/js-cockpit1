// external imports
import { CSSProperties, ReactNode } from 'react'

// --- TabsProps -----------------------------------------------------

type TabsProps = {
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports -------------------------------------------------------

export default TabsProps
