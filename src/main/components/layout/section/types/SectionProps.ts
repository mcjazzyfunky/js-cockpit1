// external imports
import React, { CSSProperties, ReactNode } from 'react'

// --- SectionProps -------------------------------------------------

type SectionProps = {
  title?: string,
  className?: string,
  style?: CSSProperties,
  children?: ReactNode 
}

// --- exports ------------------------------------------------------

export default SectionProps
